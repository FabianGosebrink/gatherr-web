export interface ContactGroup {
  groupId: string;
  message: string;
  subject: string;
}

export interface ContactGathering {
  gatheringId: string;
  message: string;
  subject: string;
}
